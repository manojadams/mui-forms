# Example 1 - default(standard)

A simple form with name, age and email.

import Form1 from "../../components/simple-forms/Form1";
import SchemaViewer from "../../components/SchemaViewer";
import schema from "../../components/simple-forms/Form1/schema.json";

<Form1 />

## Schema:

<SchemaViewer value={schema} />

## Usage:

```tsx
import React from "react";
import schema from "<schema_location>";
import MuiForms from "mui-forms";

function MySimpleForm() {
    return (
        <MuiForms
            config={{
                variant: "standard"
            }}
            schema={schema}
            onSubmit={() => {
                // do nothing
            }}
        />
    );
}
```
