# Password field

It is a text box where characters inside the box are hidden to the user.
It has similar properties with the textbox field.

import PasswordField from "../../components/fields/PasswordField";
import schema from "../../components/fields/PasswordField/schema.json";
import SchemaViewer from "../../components/SchemaViewer";

<PasswordField />

### Schema:

<SchemaViewer value={schema} />

import CommonProperties from "./common-properties.mdx";

<CommonProperties />
